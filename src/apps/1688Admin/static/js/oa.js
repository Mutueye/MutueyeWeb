$(document).ready(function(){
    
    //日历初始化
    $('#calendar').datetimepicker({
        inline: true,
        showTodayButton: true,
        viewMode: 'days',
        format: 'YYYY-MM-DD'
    });
    
});